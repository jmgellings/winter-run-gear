STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REDIRECT_URI=http://localhost:3001/auth/strava/callback

# Only needed in production deployments with a persistent volume
# (e.g. Railway). Leave unset for local development.
# DB_PATH=/data/runs.db
